{
  "name": "mirvaa-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && yarn install && yarn build && cd .. && rm -rf build && mv frontend/build build"
  },
  "engines": {
    "node": ">=18"
  }
}
